# 데이 배터리 (Day Battery) — 프로젝트 컨텍스트

> ⚠️ 이건 **웹앱(React)** 프로젝트다. 상위 폴더(Desktop)의 Flutter 플레이북은 **무시**한다.
> Claude Code 세션 시작 시 이 파일을 먼저 읽고 작업한다.

---

## 한 줄 요약

계획 템플릿(GTD·타임블로킹·불렛저널) 안 쓰고 그냥 일정만 툭툭 넣고 사는 **무계획자**를 위한 스케줄러.
각 일정이 배터리처럼 색이 빠진다 — 편안한 색으로 꽉 차 있다가 데드라인이 다가올수록 위→아래로 줄며 빨개진다.
"뭐가 코앞인지" 를 순위 매기기 없이 색과 잔량만으로 보여준다.

---

## 타겟 (항상 염두)

- **무계획자 / 즉흥형.** 시야의 끝이 "오늘, 많이 가야 내일". 일주일치 계획 세우는 사람은 타겟 아님.
- 시스템 유지관리를 안 함 → **입력은 최소, 셋업은 0.** 앱 열면 바로 씀.
- 우선순위·태그·프로젝트·예상소요 같은 칸 **만들지 않는다.** 그거 채우다 앱을 버리는 사람들임.
- 생산성 앱에 돈 쓰는 부류가 아님 → 개인 도구 / 작은 무료 툴 포지션.

## 목적

개인이 실제로 쓸 도구. 남들이 쓰면 덤. 수익 기대는 낮게.

---

## 핵심 개념: 배터리(게이지)

- **데드라인 = 그 일정의 시작 시각 그 자체.** 별도 "마감일" 칸 없음. "미팅 3시" 면 3시가 마감.
- **활주로 = 7일 (168시간).** 데드라인이 7일보다 더 남았으면 배터리는 **꽉 찬 편안한 색으로 고정**(안 움직임). 7일 안으로 들어오면 그때부터 방전 시작. (튜닝 가능한 상수 `RUNWAY_HOURS`)
- **방전**: 데드라인 7일 전(100%) → 데드라인 시각(0%). 남은시간 비율만큼 색이 차 있음.
- **색**: 60% 이상 편안한 색(청록) → 25~60% 노랑 → 25% 미만 빨강. 아래에서 위로 채워지고, 시간이 지나면 색이 **아래로 내려가며** 줄어듦(뽀모도로식).
- **날짜 배터리**: 달력의 한 칸(하루)은 그날 **제일 급한(데드라인 가장 이른) 미완료 일정** 하나를 기준으로 색이 정해진다.

### 지난 데드라인 / 완료 처리
- **데드라인 지남 + 미완료**: 텅 빈 빨강(놓침).
- **완료 체크**: 배터리 회색으로 얼림(frozen).

---

## 화면 구조

**월간 달력 하나. 화면에 그거만.** 리스트·놓침·완료 섹션 같은 거 안 만든다 (한 화면에 여러 덩어리 뜨면 혼란).

- 6주(42칸) 그리드, 일요일 시작.
- 각 날짜 칸 = 배터리. 칸 배경색이 그날 제일 급한 일정의 단계색, 세로로 잔량만큼 채워짐.
- 오늘 칸은 날짜에 검은 원 강조.
- 이번 달 아닌 날짜는 흐리게.
- (다음 단계) 날짜 칸 클릭 → 그날 일정 추가/보기 패널. 일정 추가 = 제목 + 날짜/시간, 그게 전부.
- 월간 외 뷰(주간 등)는 나중.

---

## 기술 스택

- **React + Vite + Tailwind CSS**
- 상태: 가벼우면 `useState` + Context, 커지면 Zustand 정도. (Redux 안 씀)
- **데이터 저장 = localStorage 먼저.** 백엔드/DB 없음.
- 나중 옵션: Google Calendar **읽기 전용 가져오기** (갈아타기 강요 안 함 — 기존 달력 위에 얹는 "압박 뷰").
- 배포는 나중에 (Vercel 예상).

---

## 작업 규칙

1. **프론트 먼저, 백엔드 나중.** 데이터는 목데이터로 채우고 한 곳(`src/data/mockSchedules.js`)에 모음. 실제 API 응답 형태 가정해서 구조화.
2. **시각/수치 튜닝은 한 번에 값 하나.** 색 임계값, 활주로 시간 같은 거 하나 바꾸고 멈춰서 사용자 확인 → 다음.
3. **렌더 결과를 직접 못 봄.** 색·레이아웃이 이상하면 추측으로 계속 고치지 말고 **스크린샷 요청.** 로컬 실행법(`npm run dev` → 주소) 항상 같이 안내.
4. 배터리 색·움직임은 이 앱의 정체성 → 지어내지 말고 위 "핵심 개념" 규칙대로. 바꿀 땐 먼저 확인.
5. 죽은 코드(안 쓰는 파일·함수·CSS) 보이면 정리 제안 후 확인받고 삭제.
6. 커밋은 복붙 가능한 메시지로 건네줌. 배포·커밋 등 되돌리기 어려운 작업은 명령어 먼저 보여주고 승인.

---

## 데이터 모델 (일정 하나)

```js
{
  id: string,            // uuid
  title: string,         // 제목 (유일한 필수 입력)
  deadline: string,      // ISO datetime — 일정 시작 시각 = 데드라인
  done: boolean,         // 완료 체크
}
```

- 파생값(배터리 %, 색, "놓침" 여부, 어느 칸에 속하는지)은 **저장 안 하고 렌더 시 계산.**
- 계산 로직은 `src/lib/battery.js` 한 곳에 모음.

---

## 폴더 구조 (src/)

```
src/
├── main.jsx
├── App.jsx                 # 월간 달력 화면 (지금은 여기 다 있음)
├── data/mockSchedules.js   # 목데이터 (백엔드 붙으면 교체)
├── lib/battery.js          # 배터리 % / 단계 / 날짜별 배터리 계산 (순수 함수)
└── theme/colors.js         # 배터리 단계별 색 토큰
```

> 커지면 추가 예정: `lib/storage.js`(localStorage, MVP 4단계), `components/`, `state/`.
> 지금은 파일이 적어서 굳이 안 나눔 — 죽은 폴더 만들지 않기.

---

## 미결 사항

- [x] 이름 → **데이 배터리 (Day Battery)** 확정
- [x] 타겟 → 무계획자 확정
- [x] 활주로 = 7일 (RUNWAY_HOURS)
- [x] 메인 화면 = 월간 달력 하나
- [ ] 배터리 색 3단계 정확한 HEX 값 (스크린샷 보고 튜닝)
- [ ] 칸 안에서 배터리를 어떻게 보일지 최종 (지금: 배경 세로 채움 + 날짜/제목은 흰 칩)
- [ ] 날짜 클릭 → 일정 추가/보기 패널
- [ ] 알림(브라우저 푸시)로 방전 임박 알려줄지 — MVP 이후
- [ ] Google Calendar 읽기 가져오기 — MVP 이후
- [ ] 배포 (Vercel?)

---

## MVP 개발 순서

```
1. [완료] 배터리 계산 로직 (battery.js) + 목데이터로 월간 달력 렌더
2. 색·칸 모양 다듬기 (스크린샷 확인하며 값 하나씩)
3. 날짜 칸 클릭 → 그날 일정 추가/보기 패널 (제목 + 시간)
4. localStorage 저장 (mockSchedules → 실제 저장소)
5. 완료 체크 / 삭제 / "놓침" 처리
6. 시간 흐름에 따라 배터리 실시간 방전 확인 (1분마다 갱신 — 이미 붙어있음)
```

---

*코딩 시작 전 반드시 이 파일을 읽을 것.*
